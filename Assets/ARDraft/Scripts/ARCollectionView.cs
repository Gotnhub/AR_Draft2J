using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ARCollectionView : MonoBehaviour
{
    
    Transform[] views;


    // Start is called before the first frame update
    void Start()
    {
        //views = transform.children;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
